# {{name}}

{{{description}}}

An [eve](https://eve.dev) agent app (primary surface: `{{surface}}`).

## Quickstart

```bash
pnpm install                              # from the repo root
pnpm --filter {{name}} run dev            # local TUI (needs AI_GATEWAY_API_KEY)
pnpm --filter {{name}} run eval:ci        # deterministic evals — no keys needed
pnpm --filter {{name}} run test           # unit tests
```

Copy `.env.example` to `.env.local` and fill in what you need. Deploy with
`pnpm --filter {{name}} exec eve link` then `eve deploy`.

## Docs

- Repo conventions, testing matrix, and deployment: [`../../docs/`](../../docs/)
- Eve framework docs: `node_modules/eve/docs/`
